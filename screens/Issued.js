import React from 'react';
import {View, Text} from 'react-native';

const Issued = () => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}>
			<Text>Issue UI</Text>
		</View>
	);
};

export default Issued;
